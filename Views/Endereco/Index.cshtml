@model List<EnderecoModel>

@{
    ViewBag.Subtitulo = "Endereco";
}

<table class="table table-dark">
    <thead>
        <tr>
            <th>CEP</th>
            <th>Logradouro</th>
            <th>Complemento</th>
            <th>Bairro</th>
            <th>Cidade</th>
            <th>Estado</th>
            <th>DDD</th>
            <th>Ações</th>
        </tr>
    </thead>

    <tbody>

    @foreach (var item in Model)
    {
            <tr>
                <td>@item.cep</td>
                <td>@item.logradouro</td>
                <td>@item.complemento</td>
                <td>@item.bairro</td>
                <td>@item.localidade</td>
                <td>@item.uf</td>
                <td>@item.ddd</td>
                <td>
                    <a class="btn btn-sm btn-primary" asp-action="Cadastrar" asp-route-id="@item.EnderecoId">Editar</a>
                    <a class="btn btn-sm btn-primary" asp-action="Excluir" asp-route-id="@item.EnderecoId">Excluir</a>
            </tr>
    }

    </tbody>
</table>

<a class="btn btn-primary" asp-controller="Endereco" asp-action="Cadastrar" >Adicionar</a>
<a class="btn btn-primary" asp-controller="Pedido" asp-action="Index">Retornar</a>